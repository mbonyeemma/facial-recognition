# AWS credentials (or use default profile / IAM role)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1

# S3 bucket for KYC image uploads - REQUIRED for storing images. Without it, images stay in memory only.
AWS_S3_BUCKET=your-bucket-name

# Optional
PORT=3001
